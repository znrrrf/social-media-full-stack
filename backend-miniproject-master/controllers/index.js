const userControllers = require("./userControllers");
const authControllers = require("./authControllers");
const contentControllers = require('./contentControllers')

module.exports = {
    userControllers, authControllers, contentControllers
}